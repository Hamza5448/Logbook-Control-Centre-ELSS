services:
  - type: web
    name: elss-logbook-control-centre
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT app:app
    envVars:
      - key: ELSS_SECRET_KEY
        generateValue: true
      - key: ELSS_REPORT_EMAIL
        value: chhamzaahmad6@gmail.com